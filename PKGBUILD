pkgname="froot"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="sudo alternative"
arch=('x86_64')
url=https://github.com/NopAngel/froot
license=('GPL-3.0')
depends=('gcc', 'glibc')
makedepends=('make', 'gcc')
source=("https://github.com/NopAngel/froot")
sha256sums=('SKIP')

build() {
	cd froot
	make
}
